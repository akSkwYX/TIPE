let () = print_string @@ string_of_bool @@ Checkings.is_begining_by_h_muet ( Token.Token (Word_classe.Nom, ("chatte", [])) )
let () = print_string @@ string_of_bool @@ Checkings.is_begining_by_vowel ( Token.Token (Word_classe.Nom, ("chatte", [])) )
